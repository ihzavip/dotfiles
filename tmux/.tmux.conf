# Use Ctrl-a as the prefix (optional, feel free to keep Ctrl-b)
unbind C-b
set-option -g prefix C-a
bind C-a send-prefix

# zsh
set-option -g default-shell /usr/bin/zsh

# Use vi keys in copy mode
setw -g mode-keys vi

# Enable mouse support
set -g mouse on

# Split panes
bind | split-window -h
bind - split-window -v

# Navigate panes with vim keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes with Shift + vim keys
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# New tab (window)
bind c new-window
bind n next-window
bind p previous-window

# Rename tab (optional)
bind r command-prompt "rename-window %%"

# Reload config
bind R source-file ~/.tmux.conf \; display-message "Config reloaded"

#bind-key g run-shell "tmux save-buffer - | xargs -I {} xdg-open 'https://www.google.com/search?q={}'"
#bind-key o run-shell "tmux save-buffer - | awk '{ if ($1 ~ /^https?:\\/\\// || $1 ~ /^www\\./) print $1; else print \"https://www.google.com/search?q=\" \$0 }' | xargs xdg-open"

#bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "xclip -sel clip -i"
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi V send -X select-line
bind-key -T copy-mode-vi y send -X copy-selection-and-cancel

